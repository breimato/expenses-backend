description: "Analytics Projections V1"
type: object
properties:
  projectedMonthlyExpense:
    type: string
    description: "Projected monthly expense as decimal string"
  projectedEndOfMonthBalance:
    type: string
    description: "Projected end of month balance as decimal string"
  daysRemainingInMonth:
    type: integer
    format: int32
    description: "Days remaining in the current month"
