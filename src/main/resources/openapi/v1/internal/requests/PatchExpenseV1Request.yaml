description: "Patch Expense V1 Request"
type: object
properties:
  categoryId:
    type: integer
    format: int32
    description: "Category identifier"
  amount:
    type: string
    description: "Expense amount as decimal string"
  description:
    type: string
    description: "Expense description"
  expenseDate:
    type: string
    format: date
    description: "Expense date"
  movementType:
    $ref: '../components/MovementTypeV1.yaml'
    description: "Movement direction"
  offsetsSpendingAverage:
    type: boolean
    description: >-
      Incomes: reduces net spending in analytics averages (e.g. reimbursements).
      Expenses: excludes the amount from analytics averages (e.g. savings/investments).
  reimbursedExpenseId:
    type: integer
    format: int32
    nullable: true
    description: >-
      Income only: optional link to the expense this reimbursement offsets.
      When set, offsetsSpendingAverage is forced to true. Clear with null.
