post:
  summary: "Post Recurring Applications V1"
  description: "Create recurring applications due for the current period"
  operationId: "PostRecurringTemplatesApplyPendingV1"
  tags:
    - recurring-applications-v1
  responses:
    '200':
      description: "200 OK"
      content:
        application/json:
          schema:
            $ref: '../responses/PostRecurringTemplatesApplyPendingV1Response.yaml'
    '500':
      description: "Internal Server Error"
      content:
        application/json:
          schema:
            $ref: '../components/ApiErrorV1.yaml'
